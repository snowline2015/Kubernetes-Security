apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubernetes-security.fullname" . }}-test-connection"
  labels:
    {{- include "kubernetes-security.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubernetes-security.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
