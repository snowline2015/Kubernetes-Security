apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-security.fullname" . }}
  labels:
    {{- include "kubernetes-security.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernetes-security.selectorLabels" . | nindent 4 }}
